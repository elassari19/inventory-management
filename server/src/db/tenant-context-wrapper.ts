/**
 * DEPRECATED: This file is a compatibility wrapper for tenant-context
 * Should import from src/tenant/tenant-context.ts instead
 */

import { createTenantContext, TenantContext } from '../tenant/tenant-context';

export { createTenantContext, TenantContext };
export default { createTenantContext };
